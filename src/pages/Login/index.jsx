function LoginPage() {
  return (
    <section className="page">
      <div className="page-header">
        <span className="eyebrow">Acesso</span>
        <h1>Login</h1>
      </div>

      <div className="card form-card">
        <p>Conecte o formulário de login ao Supabase Auth quando os dados do projeto estiverem configurados.</p>
      </div>
    </section>
  );
}

export default LoginPage;
